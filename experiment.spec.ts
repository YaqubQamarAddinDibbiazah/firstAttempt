import{expect, test} from '@playwright/test'

/*test('witcher purchases check', async({page}) => {
let search = page.locator('.store_nav')

    await page.goto('https://store.steampowered.com/')
    await search.getByPlaceholder('search').fill('witcher 3')
    await search.locator("//a[@id='store_search_link']//img").click()
    
}) */

// for in loop

// let character = {
//     name: 'Sonic',
//     color: 'blue',
//     speed: 1000,
// }
// console.log(character)